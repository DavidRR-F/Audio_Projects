import { KnobControlDirective } from './knob-control.directive';

describe('KnobControlDirective', () => {
  it('should create an instance', () => {
    const directive = new KnobControlDirective();
    expect(directive).toBeTruthy();
  });
});
